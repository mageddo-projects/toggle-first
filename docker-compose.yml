version: '3'
services:
  travis-build:
    image: openjdk:11-jdk
    command: ./builder.sh deploy
    working_dir: /app
    volumes:
      - ./:/app
      - $HOME:/root

  travis-pull-request-build:
    image: openjdk:11-jdk
    command: ./gradlew build
    working_dir: /app
    volumes:
      - ./:/app
      - $HOME:/root
