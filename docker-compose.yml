version: '3'
services:
  travis-build:
    image: openjdk:11-jdk
    command: |
      ./gradlew build
      ./gradlew uploadArchives
    working_dir: /app
    volumes:
      - ./:/app
      - $HOME:/root
